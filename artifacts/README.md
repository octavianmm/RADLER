# External Artifacts

Place external reproduction artifacts here when running the analysis scripts.
This directory is ignored by Git.

Expected artifact types include:

- split files;
- extracted feature tables;
- per-width IoU tables;
- trained SVM selector files;
- selected tolerance values;
- energy logs;
- generated analysis outputs.

Do not commit these files unless the authors intentionally publish them as a
separate data/model release.
